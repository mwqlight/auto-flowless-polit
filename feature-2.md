# 添加智能体流程功能

## 已完成工作

### 1. 数据库菜单配置
- 已成功执行`agent-process-menu.sql`脚本，将智能体流程菜单配置添加到flowless数据库中
- 菜单结构：
  - 智能体流程（父菜单）
  - 智能体流程列表（子菜单）
  - 智能体流程新增（按钮权限）
  - 智能体流程编辑（按钮权限）
  - 智能体流程删除（按钮权限）
  - 智能体流程发布（按钮权限）
  - 智能体流程停用（按钮权限）
- 权限分配：
  - admin角色（ID=2）：拥有所有智能体流程权限
  - root角色（ID=1）：拥有智能体流程列表、新增、编辑权限

### 2. 下一步工作
- 开发智能体流程前端模块
- 开发智能体流程后端API
- 测试智能体流程功能
1. 新增智能体流程的绘制功能，仿照现有的流程功能绘制功能，将智能体流程的绘制功能添加到项目中。核心在于嵌入执行的接入点，以及智能体的执行逻辑。智能体的执行逻辑需要根据绘制的流程进行解析和执行。智能体的执行可以是同步的，也可以是异步的，具体根据业务场景来定。 这个功能有点类似于利用流程的功能来执行智能体的功能。
2. 确保项目能够正确运行，包括后端服务，前端服务，数据库，缓存等。项目的基础信息mysql的端口为3306，账户名和密码都是root,请不要让用户输入密码执行sql操作。也就是说关于数据库的所有操作，都不需要经过用户。redis的信息是localhost:6379，没有密码。
3. 智能体的绘制功能菜单的功能和流程绘制的那一套功能是类似的，包括流程的绘制，流程的保存，流程的执行等。但是智能体的绘制功能菜单需要新增一个智能体的绘制功能，以及一个智能体的执行功能。
4. 注意，不能影响现有的所有功能，确保修改的新功能，以及原有的功能都是正常的。还有就是菜单的组织结构和流程的那一套都是一样的，只是菜单的名称是智能体流程。还有其子菜单，也是一样，相当于复刻了一套流程的功能，不过核心是用来组织智能体的执行，用来组织多个大模型，以及工具调用的执行。让流程变得更强大。